<resources>
    <!-- Base application theme. -->
    <style name="Theme.MaxShop" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/nightColorPrimary</item>

        <!-- Secondary brand color. -->
        <item name="colorAccent">@color/nightColorWhite</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/nightColorBackground</item>
        <item name="android:textColor">@color/nightColorWhite</item>
        <item name="fontFamily">@font/metropolis_regular</item>

        <item name="recyclerViewStyle">@style/BaseRecyclerView</item>
        <item name="android:textViewStyle">@style/CategoryNameTextView</item>
    </style>

</resources>